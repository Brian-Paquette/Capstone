<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exam Scheduletron</title>
        @Styles.Render("~/Content/site.css")
        @Styles.Render("~/Content/bootstrap.css")
        @Scripts.Render("~/bundles/modernizr")
</head>
<body class="app-background">
    <div class="body-content container">
        <div class="row">
            <div class="col-sm-3"></div>
            <div class="col-sm-6">
                <div class="logo-container text-shadow" style="cursor:pointer;" onclick="location.href='@Url.Action("Index", "Home")'">
                    <span class="glyphicon glyphicon-calendar" aria-hidden="true"></span> Exam Scheduletron
                </div>
                @RenderBody()
                </div>
            <div class="col-sm-3"></div>
        </div>
    </div>
            @Scripts.Render("~/bundles/jquery")
            @Scripts.Render("~/bundles/bootstrap")
            @RenderSection("scripts", required: false)
</body>
</html>
